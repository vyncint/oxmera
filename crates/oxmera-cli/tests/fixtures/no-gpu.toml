# Environment shape 1: a Linux CI runner — no Metal, modest CPU.
os = "linux"
arch = "x86_64"

[host]
memory_gb = 16.0

[toolchain]
rustc = "rustc 1.97.1 (fixture)"
cargo = "cargo 1.97.1 (fixture)"

[devices]
cpu_threads = 4
metal = false
