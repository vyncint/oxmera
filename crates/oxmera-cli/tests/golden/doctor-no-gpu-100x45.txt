size: 100x45  cursor: 31,0
oxmera doctor
=============

host: linux / x86_64
  cpu cores    4
  memory       16.0 GB unified

toolchain
  rustc        rustc 1.97.1 (fixture)
  cargo        cargo 1.97.1 (fixture)

devices
  cpu     available — 4 threads (rayon)
  metal   not available on this host
  cuda    not available on this host

capabilities
  tensor    strided views, broadcasting, batched matmul, device transfer
  dtypes    f32 everywhere; f64 on the CPU; i64 indices; no promotion
  linalg    eye diag trace, batched cholesky (differentiable) logdet det eigh
  einsum    one- and two-operand contractions with an explicit output
  indexing  index_select / index_add, native on Metal and CUDA
  autograd  reverse-mode tape, finite-difference verified, no_grad guard
  nn        Linear Conv2d Embedding LayerNorm BatchNorm2d Dropout Sequential
  losses    MSE CrossEntropy BCEWithLogits
  weights   safetensors save/load by parameter name
  optim     SGD Adam AdamW RMSprop, per-group lr/decay, fused GPU step

try: `oxmera train --tui` — the live training dashboard

doctor: report complete
