[package]
name = "b1-first-kernel"
description = "Exercise B1: a first cuda-oxide kernel with a declared launch contract. Skeleton — the implementation is the exercise."
version = "0.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

# Standalone fixture: not a workspace member. B-tier exercises pin their own
# nightly via rust-toolchain.toml in this directory; the stable root
# workspace never sees this crate.
[workspace]

[dependencies]
cuda-device = { git = "https://github.com/NVlabs/cuda-oxide", rev = "a766fc26" }
